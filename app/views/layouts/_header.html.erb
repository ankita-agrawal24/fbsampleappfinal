<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to image_tag("fb.jpeg", alt: "Facebook"), 'https://facebook.com/' %>
      <nav>
        <ul class="nav pull-rightmy">
          <% if user_signed_in? %>
             Signed in as <b><%= current_user.email %>.</b>
            <%= link_to "Profile",  edit_user_registration_path %>
            <%= link_to "Create Albums",  new_album_path %>
            <%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
          <% else %>
            <%= link_to "Home", root_path %> or 
            <%= link_to "Sign up", new_user_registration_path %> or 
            <%= link_to "Sign in", new_user_session_path %>
            <%= link_to "FConnect", "/auth/facebook" , 
                id: "sign_in" %> 
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>
